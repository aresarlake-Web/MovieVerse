# TMDB_API_KEY: Required to fetch real movie data from The Movie Database (TMDB) API.
# Without it, the app falls back to bundled mock movie data.
TMDB_API_KEY="YOUR_TMDB_API_KEY"

# APP_URL: The base URL where this app is hosted (used for self-referential links).
APP_URL="http://localhost:3000"
